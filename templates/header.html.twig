<div class="sticky top-0 z-10">
    {#<div class="bg-white" x-data="{ open: false }">
        <button @click="open = true">Ouvrir le menu</button>

        <ul
                x-show="open"
                @click.away="open = false"
        >
            Elément de menu
        </ul>
    </div>#}
    <div class="transform -translate-y-1 h-44 lg:h-48 bg-wave-nav bg-no-repeat bg-cover">
        <div class="px-10 pt-3 flex-auto lg:inline-flex  w-full">
            <a href="{{ path('app_home') }}" class=" flex-grow"><div class="text-xl lg:text-3xl font-extrabold">Mon PortFolio</div> <div class="text-yellow-500 transform lg:translate-x-48 transform lg:-translate-y-4">WIP</div></a>
            <ul class="invisible inline-flex lg:visible">
                <li><a class="font-semibold border-2 border-blue-800 hover:bg-blue-800 hover:text-white rounded-md ml-1 p-1 "
                       href="{{ path('app_listvideo') }}">Mes vidéos</a></li>
                <li><a class="font-semibold border-2 border-blue-800 hover:bg-blue-800 hover:text-white rounded-md shadow-inner ml-1 p-1 "
                       href="{{ path('app_listvideo') }}">Mes projets</a></li>
                <li><a class="font-semibold border-2 border-blue-800 hover:bg-blue-800 hover:text-white rounded-md shadow-inner ml-1 p-1 "
                       href="{{ path('app_listvideo') }}">Qui suis-je?</a></li>
            </ul>

        </div>
    </div>
</div>
